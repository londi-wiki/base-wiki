# Sample workflow for building and deploying a Hugo site to GitHub Pages
name: Deploy Hugo site to Pages
 
on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

# Default to bash
defaults:
  run:
    shell: bash

jobs:
  # Build job
  build:
    runs-on: self-hosted
    env:
      HUGO_VERSION: 0.119.0
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: get hugo modules
        run: |
          source /home/leon/.profile
          mkdir -p themes
          # git clone git@github.com:londi-wiki/dark-theme-editor.git themes/dark-theme-editor
          hugo mod get
      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"
      - name: Insert BuildTimestamp in hugo.yml
        run: |
          source /home/leon/.profile
          sed -i -e "s/2023-01-01T00:00:00+0200/$(date '+%Y-%m-%dT%H:%M:%S%z')/g" ./data/landing.yaml
          sed -i -e "s/X.Y.Z/1.0.${{ github.run_number }}/g" ./data/landing.yaml
      - name: Build with Hugo
        env:
          # For maximum backward compatibility with Hugo modules
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        run: |
          source /home/leon/.profile
          hugo --enableGitInfo

  # Deployment job
  deploy:
    runs-on: self-hosted
    needs: build
    steps:
      - name: clean folder
        run: |
          ssh ${{ secrets.WIKI_SERVER_USERNAME }}@${{ secrets.WIKI_SERVER_HOST }} 'rm -rf /var/www/html/*'
      - name: copy file via scp
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.WIKI_SERVER_HOST }}
          username: ${{ secrets.WIKI_SERVER_USERNAME }}
          key: ${{ secrets.DEPLOY_AGENT_KEY }}
          source: public/
          target: /var/www/html/
          rm: false
          strip_components: 1
      - name: Discord notification
        run: |
          curl -H "Content-Type: application/json" -X POST -d  '{ "content": "Deployment for wiki triggered.", "embeds": [ { "title": "Wiki page ✅", "description": "Wiki page successfully deployed. Run id: ${{ github.run_id }} 🥳", "url": "https://github.com/londi-wiki/base-wiki/actions/runs/${{ github.run_id }}" } ] }' https://discord.com/api/webhooks/1155459564880269382/q1MDl_diuZ0Pr8YrP_lVJf8m_lA3Cgp1ZHhlLwGqWausaOnPZ9oJS_2qi5JnHTgfi-S3 
          
